export interface FakePost {
    /**le damos los params, la forma del objeto que recibimos de la API */
    /*para propiedad opcional body?: string */
    id?: number, 
    title?: string, 
    body?: string,
    userId?: number
}
