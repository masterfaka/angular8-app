import { MiPipeResumirPipe } from './mi-pipe-resumir.pipe';

describe('MiPipeResumirPipe', () => {
  it('create an instance', () => {
    const pipe = new MiPipeResumirPipe();
    expect(pipe).toBeTruthy();
  });
});
